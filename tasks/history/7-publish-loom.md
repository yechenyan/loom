已完成

结果：
- `packages/loom` 已整理为可发布的 Python 包
- PyPI 发布名已定为 `loom-data`
- 安装后可通过 `import loom` 使用 Python API
- 安装后可通过 `loom` 使用 CLI

帮忙把 packages/loom 发布成一个 python 包

可以通过 pypi 让其他人安装
安装后可以用这个包的功能
也可以用这个当 cli 功能
