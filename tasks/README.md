# Tasks

- Put active task notes directly under `tasks/`.
- Move completed task notes into `tasks/history/`.
- When a task is archived, keep the original request and add a short implementation summary.
- Task notes are process records, not the source of truth for current behavior.
